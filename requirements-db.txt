# ControlPlane.ai — persistent storage layer (optional)
#
#   pip install -r requirements.txt -r requirements-db.txt
#
# Enables the SQLAlchemy-backed database/ module. Without these packages the
# service runs exactly as before on the in-memory managers, and the
# persistence-layer tests are skipped.

-r requirements.txt

SQLAlchemy>=2.0,<3

# PostgreSQL driver — only needed when CONTROLPLANE_DATABASE_URL points at
# Postgres (e.g. the docker-compose stack). SQLite needs no driver.
psycopg[binary]>=3.1 ; platform_system != "Windows"
