# ControlPlane.ai — core dependencies.
# Runs on a normal laptop. No GPU, no model downloads, no network calls.

pydantic>=2.6,<3
pyyaml>=6.0
pandas>=2.0
scikit-learn>=1.3       # TF-IDF vectoriser (retrieval) only
fastapi>=0.110
uvicorn>=0.27
httpx>=0.27             # FastAPI TestClient
pytest>=8.0

# Web UI — required for `python run_app.py` / `streamlit run streamlit_app.py`
# and the standalone `dashboard/app.py`.
streamlit>=1.30

# --- Optional add-ons -------------------------------------------------------
# Persistent storage layer (SQLAlchemy ORM):   pip install -r requirements-db.txt
# Next.js Command Center front end:             cd frontend && npm install
# Neither is needed to run the pipeline, the Streamlit UI, or the API in-memory.
